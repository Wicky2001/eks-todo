apiVersion: batch/v1
kind: Job
metadata:
  name: migrate-db
  namespace: app
spec:
  backoffLimit: 1
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: migration
          image: ""
          envFrom:
            - secretRef:
                name: backend-secret
